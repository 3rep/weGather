<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gather.we.dao.MypageDAO">
	<!-- <select id="getUserinfo" resultType="com.gather.we.dto.MypageDTO">
		select userid, type, username, password, email, tel, address, gender, mbti, rank, temperature, active, signupdate from users where userid=#{param1}
	</select> -->

	<!-- <select id="getUsername" resultType="String">
		select username from users where userid=#{param1}
	</select> -->
</mapper>